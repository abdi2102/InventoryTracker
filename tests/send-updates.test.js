describe("valid updates will be sent to valid sheet id for user w. write access", () => {
  test("at least one update should be submitted", () => {
    // expect error
  });

  test("updates will be sent for valid sheet id and user w. write access", () => {
    //   expect totalUpdatedRows
  });

  test("updates will not be sent for invalid sheet id or user w/o. write access", () => {
    //   expect error
  });
});
