describe("valid sheet w. valid options will return product ids for user w./ read access", () => {
  test("at least one product needs to be updated", () => {
    // expect error if numProducts is NaN or less than 1
  });

  test("return productIds if numProducts >= 1", () => {
    // expect [[productIds]]
  });

  test("an incorrect sheetId or user w/o. read access will not return productIds ", () => {
    // expect error
  });
});
